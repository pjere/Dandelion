# Development & tooling stack (tests, lint, hooks, docs). Runtime deps are in requirements.txt
# and each package's pyproject.toml. See CONTRIBUTING.md.
pytest>=8.0
ruff>=0.6
pre-commit>=3.7
pdoc>=14.0        # docstring-driven API reference — scripts/build_docs.py
openpyxl>=3.1     # read/write scenarios.xlsx in tooling
